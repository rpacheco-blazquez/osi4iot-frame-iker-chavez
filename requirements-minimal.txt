# Dependencias mínimas esenciales para el proyecto Digital Twin del Pórtico
# Versión robusta con rangos de compatibilidad
# Instalar con: pip install -r requirements-minimal.txt

# Computer Vision y Machine Learning (esenciales)
opencv-python>=4.8.0,<5.0.0
ultralytics>=8.0.0,<9.0.0
torch>=2.0.0,<3.0.0
torchvision>=0.15.0,<1.0.0
numpy>=1.24.0,<3.0.0

# Interfaz Gráfica (esencial)
PyQt5>=5.15.0,<6.0.0

# Comunicación (esencial)
paho-mqtt>=1.6.0,<3.0.0
requests>=2.28.0,<3.0.0

# Manejo de Datos (esencial)
pandas>=2.0.0,<3.0.0

# Configuración (esencial)
PyYAML>=6.0.0,<7.0.0

# Visualización básica (esencial)
matplotlib>=3.7.0,<4.0.0

# Utilidades del sistema (esenciales)
tqdm>=4.65.0,<5.0.0
psutil>=5.9.0,<8.0.0

# Dependencias específicas para compatibilidad
setuptools>=65.0.0
wheel>=0.40.0

# Nota: Las demás dependencias se instalarán automáticamente como dependencias
# de los paquetes principales listados arriba.
# 
# Instrucciones de instalación:
# 1. python -m pip install --upgrade pip
# 2. pip install -r requirements-minimal.txt